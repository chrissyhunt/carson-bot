<div class="jumbotron">
  <div class="inside-header">
    <h1><%=@user.name%>'s Items by Location</h1>
  </div>

  <div class="inside-nav">
    <%= erb :nav %>
  </div>
</div>

<div class="container">
  <div class="row inside-content">

    <div class="col-sm-8 info-box">
      <h4>Inventory by Location</h4>
      <div class="long-list">
        <% @user_locations.each do |location| %>
        <div>
          <p><a href="/locations/<%=location.id%>"><%=location.name%></a> (<a href="/locations/<%=location.id%>/edit">edit</a>)</p>
            <ul>
              <% location.items.each do |item| %>
                <li><%=item.name if @user.items.include?(item)%></li>
              <% end %>
            </ul></div>
          <% end %>
      </div>
    </div>

    <div class="col-sm info-box col-right">
      <%= erb :sidenav %>
    </div>

  </div>
</div>
